# ============================================
# Routing Settings
# ============================================
artifacts_root: artifacts

data_ingestion:
  root_dir: artifacts/data_ingestion
  source_zenodo_id: "4740355"
  data_dir: artifacts/data_ingestion/mimii_due

data_transformation:
  root_dir: artifacts/data_transformation
  processed_dir: artifacts/data_transformation/processed

data_validation:
  root_dir: artifacts/data_validation
  status_file: artifacts/data_validation/status.txt
  schema_path: schema.yaml

model_trainer:
  root_dir: artifacts/model_trainer
  model_path: artifacts/model_trainer/autoencoder_best.pth
  metrics_path: artifacts/model_trainer/training_log.json

model_evaluation:
  root_dir: artifacts/model_evaluation
  metrics_path: artifacts/model_evaluation/metrics.json
  roc_path: artifacts/model_evaluation/roc_curves.png

quantization:
  root_dir: artifacts/quantization
  golden_weights: artifacts/quantization/golden_weights.json
  activation_ranges: artifacts/quantization/activation_ranges.json

rtl_verification:
  root_dir: artifacts/rtl_verification
  test_vectors: artifacts/rtl_verification/test_vectors_16f.json

experiments_log: artifacts/experiments/experiments.json
